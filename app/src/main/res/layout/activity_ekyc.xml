<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>
        <variable
            name="isLoading"
            type="Boolean" />
        <import type="android.view.View" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:padding="24dp"
        tools:context=".EkycActivity">

        <androidx.cardview.widget.CardView
            android:id="@+id/card_face_image"
            android:layout_width="0dp"
            android:layout_height="200dp"
            app:cardCornerRadius="16dp"
            app:cardElevation="6dp"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            android:layout_marginTop="32dp">

            <ImageView
                android:id="@+id/image_face"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:scaleType="centerCrop"
                android:contentDescription="Ảnh khuôn mặt người dùng" />
        </androidx.cardview.widget.CardView>

        <Button
            android:id="@+id/button_select_face"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:text="Chọn ảnh khuôn mặt"
            android:backgroundTint="#005792"
            android:textColor="@android:color/white"
            app:cornerRadius="10dp"
            android:layout_marginTop="32dp"
            app:layout_constraintTop_toBottomOf="@id/card_face_image"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent" />

        <Button
            android:id="@+id/button_verify_ekyc"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:text="Xác thực eKYC"
            android:backgroundTint="#003366"
            android:textColor="@android:color/white"
            app:cornerRadius="10dp"
            android:visibility="@{isLoading ? View.GONE : View.VISIBLE}"
            android:layout_marginBottom="24dp"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent" />

        <ProgressBar
            android:id="@+id/progress_bar"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:visibility="@{isLoading ? View.VISIBLE : View.GONE}"
            app:layout_constraintTop_toTopOf="@id/button_verify_ekyc"
            app:layout_constraintBottom_toBottomOf="@id/button_verify_ekyc"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent" />

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>